{{- if .Values.bridge.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pilox-transport-bridge.fullname" . }}-bridge
  labels:
    {{- include "pilox-transport-bridge.labels" . | nindent 4 }}
    app.kubernetes.io/component: bridge
spec:
  type: {{ .Values.bridge.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "pilox-transport-bridge.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: bridge
  ports:
    - name: http
      port: {{ .Values.bridge.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
